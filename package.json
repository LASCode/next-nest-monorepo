{
  "name": "task-next-nest",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "lerna:start": "npx lerna run _lerna:start --stream",
    "lerna:bootstrap": "npx lerna bootstrap",
    "lerna:build": "npx lerna run build"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "lerna": "^6.5.1"
  }
}
